import { Container } from "./Appbar.styled";
import { Navigation } from "../Navigation/Navigation";

export const AppBar = () => {
  return (
    <Container>
      <Navigation />
    </Container>
  );
};